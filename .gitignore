# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files / secrets (never commit CXone keys or tokens)
.env
.env.*
!.env.example
# .env.production holds only non-secret public-client config (see the file header)
# and is committed on purpose so the GitHub Pages build is reproducible.
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
